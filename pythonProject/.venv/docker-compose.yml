version: '3.8'

services:
  bot:
    build: .
    container_name: telegram-notification-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
      - DEBUG=${DEBUG}
    volumes:
      - ./logs:/app/logs